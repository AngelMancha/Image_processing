#include "progargs.cpp"
#include "imagesoa.cpp"
//#include <ctime>

int main(int argc, char *argv[]) {
    errores(argc,argv);
    //medir el tiempo que va a tardar en abrir el archivo
    /*unsigned time_start_load, time_end_load;
    time_start_load = clock();
    DIR *dirin = opendir(argv[0]);
    time_end_load = clock();*/

    //incluir la funcion que se va a ejecutar
    return 0;
}
