#include "gtest/gtest.h"
#include "../soa/Image_soa.h"
#include "../aos/Image_aos.h"
#include "../common/common.h"

TEST(Google_tests, test1) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(Google_tests, test2) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}